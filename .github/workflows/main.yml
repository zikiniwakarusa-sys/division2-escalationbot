name: division2 bot

on:
  schedule:
    - cron: "0/5 * * * *"
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5  # ←ここだけ変更

      - run: pip install requests

      - run: python script.py
